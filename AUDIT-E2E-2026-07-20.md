# AUDIT E2E PRODUKSI — 20 Julai 2026 (bakwim.my)

**Konteks:** pemilik lapor 3 kegagalan (upload UI, intake e-mel, notifikasi Telegram) + minta audit
menyeluruh dengan bukti sebenar (bukan "page boleh akses = lulus"). Semua diuji LIVE di produksi
(`commit 01aa19c`) dengan bukti rangkaian + semakan DB + hantaran notifikasi sebenar.

## 3 bug dilapor + 1 bug baharu — SEMUA DIBAIKI & DISAHKAN LIVE

| Bug | Punca sebenar (bukti kod) | Fix | Bukti LIVE |
|---|---|---|---|
| **A. Upload UI gagal** | `config/livewire.php` tiada → temp Livewire guna disk lalai `cos` (S3) → pelayar PUT pra-tandatangan ke COS tanpa CORS → gagal | `config/livewire.php` `temp.disk=local` | **Rangkaian: `POST /livewire/upload-file` = 200** (bukan COS); rekod #20 di DB (`source=muat_naik, ocr=siap`); media `disk=cos wujud=true` |
| **B. E-mel intake tersekat** | Kunci job `WithoutOverlapping('diwan-fetch-mail')` tanpa expiry (`expiresAfter=0`) KEKAL selepas container di-recreate mid-run → fetch-mail dilangkau selamanya | `expireAfter(600)` + scheduler `withoutOverlapping(10)` + lepaskan kunci tersekat | E-mel pemilik (rekod #18) kini di Peti Masuk MAMAD; IMAP unseen=0 |
| **C. Telegram tak sampai** | `notify_telegram` lalai DB=false; webhook `/start` hanya simpan `chat_id` → `via()` SKIP Telegram walau "Bersambung" | `/start` set `notify_telegram=true` + wrapper log + TestNotification `toTelegram` | **`NotificationLog` telegram `status=sent` to=667224545** (before=0 — buktikan gap dulu); dashboard "Telegram OK @spdmediwanbot" |
| **E. OCR imej gagal** (baharu) | `img2pdf` ABORT pada tag putaran EXIF tak sah (foto/skrinsyot telefon) | `img2pdf --rotation=ifvalid` | Rekod #18 (JPEG e-mel) kini `ocr=siap len=391` (dulu `gagal`) |

**Penambahan intake e-mel (permintaan pemilik):** mana-mana pengirim ke `scan+{slug}` kini DITERIMA
(submission awam, had 10/jam) — allowlist = pengirim dipercayai (100/jam). Lalai `allow_public=true`.

## Fasa audit (semua LIVE, bukti sebenar)

### Awam / Log masuk
- ✔ Magic-link **interstisial + fix bounce**: 2 log masuk berjaya tanpa bounce (admin-smoke, superadmin) walau sesi lain aktif — auto-submit JS.
- ✔ UI bergaya penuh (bulan sabit, tema Filament) — TIADA UI pecah.

### Tenant (smoke) — workflow sebenar
- ✔✔ **Upload (Bug A)**: Peti Masuk → Muat Naik `.txt` → FilePond "Upload complete" → Hantar → rekod #20 muncul (📤 Muat Naik) → DB+COS+OCR disahkan. **Bukti rangkaian membuktikan temp→local, bukan COS.**
- ✔ Peti Masuk: aksi Lihat/Klasifikasikan/Padam render; modal upload (format sah dipaparkan).
- ✔ Carian: borang render, dropdown fail **skop-tenant** (hanya `04D588` smoke = isolasi); rekod Peti Masuk belum difailkan tidak muncul (reka bentuk betul).
- ✔ Tetapan Masjid: kad intake + **teks baharu "Semua pengirim (had lebih rendah)"** (WP2) LIVE; alamat `scan+smoke@bakwim.my`.

### Superadmin
- ✔ Dashboard **Kesihatan Saluran**: Gateway OK · **IMAP OK** (WP2) · Sesi WA 1/1 · **Telegram OK @spdmediwanbot** (stat WP3 baharu).
- ✔ Ringkasan Platform: 2 masjid, 7 pengguna, 19 rekod, 2 pesanan belum bayar.
- ✔ Status Sambungan: Gateway/IMAP OK; sesi WA (Platform Dimatikan, MAMAD Bersambung 60176811605).

### Notifikasi saluran sebenar (Fasa N)
- ✔✔ TestNotification → admin MAMAD → **Telegram dihantar sebenar** (log `sent`, chat 667224545).

## Liputan & kejujuran
Diuji LIVE dengan bukti keras: 4 bug + magic-link + upload E2E + tetapan + dashboard + carian + status
+ hantaran Telegram sebenar. Workflow TIDAK berubah sesi ini (modal klasifikasi medan-dinamik penuh,
minit, kelulusan, pelupusan) telah disahkan E2E pada sesi 19–20 Jul terdahulu + diliputi **Pest 342✓**.
Modal klasifikasi guna medan reaktif Livewire (`wire:model.blur`) yang sukar dipandu automasi pelayar —
render medan disahkan; logik difailkan diliputi ujian + sesi lepas.

## Artifak ujian
- Rekod #20 (smoke, `.txt` ujian) — kekal sebagai data demo tenant ujian (bukan tenant sebenar).
- 1 notifikasi Telegram ujian ke akaun pemilik (sah — bukti Bug C).

**Keputusan:** `local = origin = server = 01aa19c` · Pest **342✓/1 skip** · CI HIJAU · /up 200 · smoke 9/9 · 7 container · failed_jobs 0.
